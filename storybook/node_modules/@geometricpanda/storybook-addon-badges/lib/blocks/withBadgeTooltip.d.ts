import React, { FC, ComponentType } from 'react';
import type { BadgeProps } from '../types';
export declare const WithBadgeTooltip: <T extends BadgeProps = BadgeProps>(Component: React.ComponentType<T>) => React.FC<T>;

import { Meta, Canvas, Story } from "@storybook/addon-docs/blocks";
import data from "../assets/data/tokens.json";

import { BADGE } from "@geometricpanda/storybook-addon-badges";

<Meta title="Design Tokens/Typography" parameters={{}} />

# Typography

Use Typography tokens to style font sizes, family, letter-spacing and line height of your font.

## Font Size

<Story name="Font Size">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.size).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <p
                style={{ fontSize: `${[token.value]}`, wordBreak: "break-all" }}
              >
                Aa
              </p>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

## Font Family

<Story name="Font Family">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.family).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <p
                style={{
                  fontFamily: `${[token.value]}`,
                  wordBreak: "break-all",
                }}
              >
                Aa
              </p>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

## Font Weight

<Story name="Font Weight">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.weight).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <p
                style={{
                  fontWeight: `${[token.value]}`,
                  wordBreak: "break-all",
                }}
              >
                Aa
              </p>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

## Line Height

<Story name="Line Height">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.lineHeight).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <p
                style={{
                  lineHeight: `${[token.value]}`,
                  wordBreak: "break-all",
                }}
              >
                ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz1234567890
              </p>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

## Letter Spacing

<Story name="Letter Spacing">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.letterSpacing).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <p
                style={{
                  letterSpacing: `${[token.value]}`,
                  wordBreak: "break-all",
                }}
              >
                ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz1234567890
              </p>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

## Headings

### H1

<Story name="H1">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.headings.one).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <h1>Aa</h1>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

### H2

<Story name="H2">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.headings.two).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <h2>Aa</h2>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

### H3

<Story name="H3">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.headings.three).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <h3>Aa</h3>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

### H4

<Story name="H4">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.headings.four).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <h4>Aa</h4>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

### H5

<Story name="H5">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.headings.five).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <h5>Aa</h5>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

### H6

<Story name="H6">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.headings.six).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <h6>Aa</h6>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

### Display 01

<Story name="Display 01">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.display.one).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <p className="made-u-display--01">Aa</p>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

### Display 02

<Story name="Display 02">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.display.two).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <p className="made-u-display--02">Aa</p>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

### Display 03

<Story name="Display 03">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.display.three).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <p className="made-u-display--03">Aa</p>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

### Eyebrow 01

<Story name="Eyebrow 01">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.eyebrow.one).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <p className="made-u-eyebrow--01">Aa</p>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

### Eyebrow 02

<Story name="Eyebrow 02">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.eyebrow.two).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <p className="made-u-eyebrow--02">Aa</p>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>

## Body Text

<Story name="Body">
  <table className="made-c-table made-s-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Role</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(data.font.body).map(([id, token], index) => {
        return (
          <tr key={index}>
            <td>
              <code>{token.name}</code>
              <small> Themeable: {token.themeable} </small>
            </td>
            <td>{token.comment}</td>
            <td>
              <p style={{
                  fontSize: `${[token.value]}`,
                  fontWeight: `${[token.value]}`,
                  fontFamily: `${[token.value]}, MarkOffcForMC`,
                  lineHeight: 1.5,
              }} >
                Aa
              </p>
              <code>{token.original}</code>
            </td>
          </tr>
        );
      })}
    </tbody>
  </table>
</Story>
